package constants

const BuildDir = "build"
const configPrefix = "config"
const ConfigFileName = configPrefix + ConfigExtension
const GameConfigFileName = configPrefix + ".%s" + ConfigExtension
const ConfigExtension = ".toml"
